export function registerPrefsScripts(_window: Window) {
  // The template automatically handles preferences linked in the XHTML.
  // No custom script is needed for simple preferences.
  // This function is kept for potential future use.
}
